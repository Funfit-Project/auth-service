server:
  port: 8081

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka-discovery:8761/eureka

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://auth_mysql:3306/auth?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: thgml0108^^*! # ${DATASOURCE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
    exchange: funfit

logging:
  level:
    org:
      hibernate.SQL: debug
jwt:
  secret: flwkegnwoirgnoaeringaleriknglakernglekrng # ${JWT_SECRET}

springdoc:
  packages-to-scan: funfit.auth
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8
  swagger-ui:
    path: /
    disable-swagger-default-url: true
    display-request-duration: true
    operations-sorter: alph
